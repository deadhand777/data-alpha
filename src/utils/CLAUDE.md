# CLAUDE.md - Utils

`src/utils/` empty right now — no files besides this one. No established structure or conventions yet.

When adding a utility here: keep it generic/reusable (project-domain-specific code belongs in `src/lib/` instead). Pure functions, named exports for tree-shaking.
