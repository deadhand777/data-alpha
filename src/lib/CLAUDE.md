# CLAUDE.md - Lib

`src/lib/` empty right now — no files besides this one. No established structure or conventions yet.

When adding code here: shared types, constants, or utilities tightly coupled to this project's domain (vs. `src/utils/` for generic reusable helpers).
