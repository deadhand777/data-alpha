# CLAUDE.md - Layouts

`src/layouts/` empty right now — no files besides this one. The actual page layout (`Layout.astro`) currently lives in `src/components/`, not here.

If adding dedicated layouts, move `Layout.astro` here first and update imports across `src/pages/`.
