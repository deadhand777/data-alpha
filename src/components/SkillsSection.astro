---
interface ExpertiseGroup {
  title: string;
  description: string;
  tools: string[];
}

interface Props {
  expertise: ExpertiseGroup[];
}

const { expertise } = Astro.props;
---
{expertise.length > 0 && (
  <section class="mb-16" aria-labelledby="expertise-heading">
    <div class="mb-8 max-w-2xl">
      <p class="mb-2 text-sm font-semibold uppercase tracking-wide text-accent-tech-light">Capabilities</p>
      <h2 id="expertise-heading" class="text-3xl font-bold text-text-primary">What I Build</h2>
    </div>

    <div class="grid gap-5 md:grid-cols-2">
      {expertise.map((group) => (
        <article class="rounded-lg border border-white/5 bg-bg-surface p-6 shadow-resting transition-colors duration-300 hover:border-white/10">
          <h3 class="text-lg font-bold text-text-primary">{group.title}</h3>
          <p class="mt-3 leading-7 text-text-secondary">{group.description}</p>
          {group.tools.length > 0 && (
            <ul class="mt-5 flex flex-wrap gap-2" aria-label={`${group.title} technologies`}>
              {group.tools.map((tool) => (
                <li class="rounded-full bg-bg-surface-light px-3 py-1 text-xs font-medium text-text-secondary">{tool}</li>
              ))}
            </ul>
          )}
        </article>
      ))}
    </div>
  </section>
)}
